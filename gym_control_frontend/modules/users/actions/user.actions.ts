export const userActionCatalog = [
  "edit_user",
  "deactivate_user",
  "soft_delete_user",
  "restore_user",
  "change_role",
  "suspend_access",
  "bulk_deactivate_users",
  "bulk_delete_users",
] as const;
